name: Trigger auto deployment for thesis

on:
  push:
    branches: 
      - main
    paths:
      - '**'
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write
      contents: read
      packages: write

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v4

      # Step 1: Log directly into GitHub Container Registry
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Step 2: Build the image with your lowercase username forced
      - name: Build and push image to GHCR
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ghcr.io/simarjit1303/rag-adversarial-robustness/thesis:${{ github.sha }}

      # Step 3: Log into your Azure account
      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.THESIS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.THESIS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.THESIS_AZURE_SUBSCRIPTION_ID }}

      # Step 4: Link GHCR credentials to Azure and pull the image
      - name: Deploy to Azure Container Apps
        uses: azure/CLI@v2
        with:
          inlineScript: |
            az extension add --name containerapp --upgrade
            
            # 1. Give Azure your GitHub token so it is authorized to pull from GHCR
            az containerapp registry set \
              --name thesis \
              --resource-group Master-Thesis \
              --server ghcr.io \
              --username ${{ github.actor }} \
              --password ${{ secrets.GITHUB_TOKEN }}
            
            # 2. Deploy the updated image
            az containerapp update \
              --name thesis \
              --resource-group Master-Thesis \
              --image ghcr.io/simarjit1303/rag-adversarial-robustness/thesis:${{ github.sha }}
